Enzyme 1 gap analysis complete. Score was 52/100 with 14 missing/broken features and 11 partial.

Now run Enzyme 2 — IMPLEMENTATION PLAN AND REPAIR.

Using the gap analysis above, create an atomic, prioritized implementation plan that closes every gap.

Priority 1 — BLOCKERS (must fix before launch):
For each missing/broken feature:
- What: Exact description of the gap
- Where: Specific files, functions, components  
- How: Step-by-step fix with exact code changes
- Test: How to verify the fix works

Priority 2 — GAPS (should fix before launch):
Same format for partial features.

Rules:
- Every fix must be atomic — one clear change, one clear test
- Order by dependency
- Include exact file paths and function names
- No vague instructions — be specific
- Estimate minutes per fix

Then IMPLEMENT all Priority 1 fixes directly. Fix the code. Commit if there is a git repo.

Final answer: After all Priority 1 fixes are implemented, is Windy Pro ready for a real user to download and