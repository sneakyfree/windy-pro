# Windy Pro Translation - Training Requirements
# Install: pip install -r requirements.txt

# Core training dependencies
torch>=2.0.0
transformers>=4.36.0
peft>=0.7.0  # Parameter-Efficient Fine-Tuning (LoRA)
accelerate>=0.25.0

# Dataset handling
datasets>=2.16.0
sentencepiece>=0.1.99  # M2M-100 tokenizer

# Evaluation metrics
evaluate>=0.4.1
sacrebleu>=2.3.1  # BLEU score
datasets>=2.16.0  # For chrF++ via evaluate

# Data processing
numpy>=1.24.0
pandas>=2.0.0
pyyaml>=6.0

# Progress tracking
tqdm>=4.66.0

# Optional: W&B for experiment tracking
# wandb>=0.16.0

# Optional: Better model export
safetensors>=0.4.0
