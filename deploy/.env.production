# ═══════════════════════════════════════════════════════
# Windy Pro — Production Environment Variables
# Copy this file to .env and fill in the values.
# ═══════════════════════════════════════════════════════

# Required: API key for server authentication
WINDY_API_KEY=change-me-to-a-strong-random-key

# Required: JWT secret for user auth tokens (min 32 chars)
WINDY_JWT_SECRET=change-me-to-a-32-plus-char-secret-key

# Required: PostgreSQL password
POSTGRES_PASSWORD=change-me-strong-db-password

# Optional: Override default API host/port
# API_HOST=0.0.0.0
# API_PORT=8000

# Optional: Redis URL (defaults to redis://redis:6379/0 in Docker)
# REDIS_URL=redis://redis:6379/0

# Optional: Whisper model size (tiny, base, small, medium, large-v3, large-v3-turbo)
# WHISPER_MODEL=base
